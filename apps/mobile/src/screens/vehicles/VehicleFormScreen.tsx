import { PlaceholderScreen } from '../../components/ui/PlaceholderScreen';

export function VehicleFormScreen() {
  return (
    <PlaceholderScreen
      title="Cadastrar veiculo"
      description="Placeholder do formulario com make, model, year, fuelType e currentMileage."
    />
  );
}
